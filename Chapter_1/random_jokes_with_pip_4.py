# What is pip? pip is a package manager for Python.
#  It allows you to install and manage additional libraries and dependencies 
# that are not included in the standard Python library. 
# With pip, you can easily install packages from the Python Package Index
#  (PyPI) and other repositories, making it easier to use third-party libraries in your Python projects.

import random

jokes = [
	"Why do programmers prefer dark mode? Because light attracts bugs.",
	"Why did the programmer quit his job? Because he didn't get arrays.",
	"How many programmers does it take to change a light bulb? None, that's a hardware problem."
]

print(random.choice(jokes))